// import React from 'react';
// import textVariants from './textVariants';

// interface TextProps {
//   intent: 'head' | 'subTitle' | 'base';
//   children: React.ReactNode;
// }

// const Text: React.FC<TextProps> = ({ intent, children }) => {
//   const classNames = textVariants({ intent });

//   return <p className={classNames}>{children}</p>;
// };

// export default Text;
